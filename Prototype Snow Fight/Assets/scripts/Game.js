#pragma strict
var teams = 2;
private var score = new Array();

function Start () {
	score.length = teams;
}

function Update () {
	//display score
}

function UpdateScore(scoringTeam : int) {
	//score[scoringTeam]++;
}